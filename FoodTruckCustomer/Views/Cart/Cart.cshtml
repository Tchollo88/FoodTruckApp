@model Repository.Models.Menu.Order
@{
    Layout = "_Layout";
}

<h1>Your Cart</h1>

@if (Model?.LineItems.Count == 0)
{
    <p>Your cart is empty. <a href="@Url.Action("Items", "Customer")">Add some items!</a></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Item</th>
                <th>Price</th>
                <th>Quantity</th>
                <th>Subtotal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var lineItem in Model.LineItems)
            {
                <tr>
                    <td>@lineItem.Item.Name</td>
                    <td>@lineItem.Item.Price.ToString("C")</td>
                    <td>
                        <form asp-action="AdjustQuantity" asp-controller="Cart" method="post">
                            <input type="hidden" name="lineItemId" value="@lineItem.lineItem_ID" />
                            <button type="submit" name="qty" value="1" class="btn btn-primary"> [+] </button>
                            <button type="submit" name="qty" value="-1" class="btn btn-primary"> [-] </button>
                        </form>
                    </td>
                    <td>@(lineItem.Quantity * lineItem.Item.Price).ToString("C")</td>
                    <td>
                        <form asp-action="Delete" asp-controller="Cart" method="post">
                            <input type="hidden" name="lineItemId" value="@lineItem.lineItem_ID" />
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3"><strong>Total:</strong></td>
                <td><strong>@Model.SubTotal.ToString("C")</strong></td>
                <td>
                    <a asp-action="Checkout" asp-controller="Receipts" asp-route-orderId="@Model.Order_ID" class="btn btn-success">Proceed to Checkout</a>
                </td>
            </tr>
        </tfoot>
    </table>
}

@*@using Repository.Models.Menu;
@using FoodTruckCustomer.Controllers;

﻿@{
    Layout = "_Layout";
}

@model IEnumerable<Repository.Models.Menu.Order>


@foreach(var order in Model)
{
    var receipt = new Repository.Models.Menu.Receipt
				{
					Receipt_ID = order.Order_ID,
					Order_ID = order.Order_ID,
					Date = DateTime.Now,
					Order = order,
				};
	receipt.Order = order;

	var recID = order.Order_ID;
}

@{
    ViewData["Title"] = "Cart";
}

<h2>Shopping Cart</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Order Number</th>
            <th>Items</th>
            <th>Subtotal ($)</th>
            <th>Actions</th>
			<td></td> @*-- Empty cell for Delete -->*@
        @*@</tr>
    </thead>
    @foreach (var order in Model)
    {
        <tbody>
            <tr>
                @foreach (var item in order.LineItems)
                {
                    <td>@item.lineItem_ID</td>
                }
                <td>
                    @foreach (var item in order.LineItems)
                    {
                        <ul>
                            <li>@item.Item.Name - Quantity: @item.Quantity - Price: $@(item.Item.Price * item.Quantity)</li>
                        </ul>
                    }
                </td>
                <td>@order.SubTotal.ToString("C")</td>
                @foreach (var item in order.LineItems)
                {
                    <td>
                        <form asp-action="AdjustCount" asp-controller="Cart" asp-route-lineItemId="@item.lineItem_ID" method="post">
                            <input type="hidden" name="orderId" value="@order.Order_ID" />
                            <button type="submit" name="qty" value="1" class="btn btn-primary"> [+] </button>
                            <button type="submit" name="qty" value="-1" class="btn btn-primary"> [-] </button>
                        </form>
                    </td>
                    <td>
                        <form asp-action="Delete" asp-controller="Cart" method="post">
                            <input type="hidden" name="lineItemId" value="@item.lineItem_ID" />
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </td>
                } 
            </tr>
        </tbody>
    }
    <tfoot>
        <tr>
            <td colspan="2"><strong>Total:</strong></td>
            <td><strong>@Model.Sum(o => o.SubTotal).ToString("C")</strong></td>
            <td>
                @foreach(order in mo)
                <form asp-action="Checkout" asp-controller="Receipts" asp-route-lineItemId="receipt.lineItem_ID" method="post">
                    <input type="hidden" name="orderid"  value="receipt" />
                    <button type="submit" class="btn btn-primary">Checkout</button>
                </form>
            </td>
        </tr>
    </tfoot>
</table>

<form asp-action="TransferViewPost" method="post">
    <button type="submit">Return to Menu</button>
</form>*@



